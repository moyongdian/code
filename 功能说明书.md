# 校园外卖点餐平台 —— 功能说明书（Go 语言实现版）

> 本说明书基于现有校园外卖/点餐系统解析编写，**后端由 Java Spring Boot 重构为 Go 语言实现**，前端（Vue 管理后台 + UniApp 移动端）保持不变。本文以功能清单 + Go 技术实现方案的方式呈现，可直接作为使用 Go 重建同类项目的需求、设计与开发依据；**第 10 章给出了对现有功能的专业审视与优化建议，重建时建议一并采纳。**

> 读取建议：`4`～`7` 章为基线功能需求（原样可实现），`10` 章为升级项（P0 必修、P1 建议、P2 可选）。

---

## 1. 项目概述

本项目是一个**校园外卖 / 在线点餐平台**，采用前后端分离 + 移动端多端方案，覆盖**用户点餐、商家经营、骑手配送、平台管理**四条业务线。

系统由三个部分组成：

| 组成 | 技术方案 | 使用方 | 职责 |
|---|---|---|---|
| 移动端（用户 + 骑手） | UniApp（Vue 2），可发布 Android App / 微信小程序等多端 | 学生用户、骑手 | 浏览商家、下单支付、评价、收藏、收货地址、实名认证、骑手接单 |
| PC 管理后台 | Vue 2 + Element UI + ECharts + wangeditor | 管理员、商家 | 用户/商家/商品/分类/订单管理、新闻公告、数据统计、Excel 导入导出、系统操作日志 |
| 后端服务 | **Go (Gin) + GORM + MySQL + JWT + 中间件** | 服务端 | 统一 RESTful 接口、鉴权、文件存储、业务逻辑、Excel 处理 |

数据库：MySQL（库名 `honey2024`），后端端口 `9090`。

---

## 2. 角色与权限模型

| 角色 | 系统内权限 |
|---|---|
| 管理员 | PC 后台全部功能：用户管理、管理员管理、商家审核、商品/分类管理、订单、新闻、公告、日志、数据统计 |
| 商家 | 管理自己的店铺：商品、分类、订单出餐发货（对应订单状态由商家侧推进） |
| 用户（学生） | 移动端：浏览、搜索、收藏、加购、下单、支付、评价、地址管理、个人信息 |
| 骑手（配送员） | 移动端：实名认证后进入接单页面，接收"已出餐"订单并配送 |

权限控制要点：
- 后端所有业务接口由 **JWT 中间件**统一鉴权（登录/注册/文件下载等白名单接口除外）。
- Gin 中通过路由分组区分：**公开分组**（登录、注册、文件上传下载/预览）挂载到白名单中间件，**受保护分组**统一走 JWT 鉴权中间件；业务接口再按 `role` 做角色校验。
- 前端管理后台通过菜单 `v-if="user.role === '管理员'"` 控制功能可见性（路由守卫未启用）。

---

## 3. 技术栈清单

### 3.1 后端（Go）
- **语言/运行时**：Go 1.21+（单二进制部署，跨平台编译）
- **Web 框架**：Gin（高性能，路由分组、中间件、参数绑定、统一错误处理）
- **ORM**：GORM（自动建表 Migrate、事务、关联预加载 Preload、软删除）
- **数据库**：MySQL 8（`github.com/go-sql-driver/mysql`）
- **配置**：Viper + YAML（端口、数据库连接、JWT 密钥、上传目录、文件访问域名）
- **JWT 鉴权**：`github.com/golang-jwt/jwt/v5`
- **密码安全**：`golang.org/x/crypto/bcrypt`（注册/改密时哈希存储）
- **Excel 导入导出**：`github.com/xuri/excelize/v2`
- **雪花订单号**：`github.com/sony/sonyflake`（或 `bwmarrin/snowflake`）
- **金额计算**：`github.com/shopspring/decimal`（金额一律 decimal，避免浮点误差）
- **日志**：`logrus` / `zap` + Gin 操作日志中间件
- **CORS**：`github.com/gin-contrib/cors`
- **表单/参数校验**：`github.com/go-playground/validator/v10`（struct tag 校验）
- **定时任务（可选）**：`robfig/cron/v3`（超时未支付订单自动取消、销售统计定时聚合）

### 3.2 管理后台（myvue，不变）
- Vue 2.5 + Vue Router 3 + Axios 1.6
- Element UI 2.15（表格、表单、弹窗、分页、上传）
- ECharts 5.4（数据图表统计）
- wangeditor 4.7（富文本编辑，支持图片/视频上传播放，highlight.js 代码高亮）
- Webpack 3 构建，页面路由懒加载

### 3.3 移动端（uniapp，不变）
- UniApp（Vue 2），目标平台：Android 5+ App、微信/支付宝/百度/头条小程序
- 底部 TabBar：首页 / 订单 / 我的
- 首页轮播、公告轮播、商家列表、购物车、订单状态机、评价星级组件（uni-rate）、表单校验组件

### 3.4 Go 后端工程结构（建议分层）

```
server/
├── main.go                 # 入口：加载配置、初始化 DB、注册路由、启动服务
├── config/                 # 配置结构体 + Viper 加载（config.yaml）
├── router/                 # 路由注册与分组（公开组 / 鉴权组 / 管理员组）
├── middleware/             # JWT 鉴权、操作日志、CORS、Recovery
├── models/                 # GORM 模型（对应数据表）+ 请求/响应 DTO
├── dao/  (repository)      # 数据访问层（GORM 封装，分页/查询）
├── services/               # 业务逻辑层（下单事务、金额计算、状态流转）
├── controllers/ (handlers) # HTTP 处理层：参数绑定→调 service→统一响应
├── utils/                  # JWT、雪花ID、IP 解析、文件存储、Excel 读写
└── middlewares/logs        # 操作日志表写入
```

---

## 4. 功能模块详解

### 4.1 通用/基础功能（后端公共服务）

**（1）账号注册与认证**
- 管理后台注册（`/register`）：用户名、密码、角色
- 移动端注册（`/appRegister`）：强制角色为"用户"
- 登录（`/login`）：校验用户名密码，签发 JWT token（有效期 2 小时），用户信息含 `token` 返回
- 忘记/重置密码（`/password`）：通过"用户名 + 手机号"验证后重置为固定密码 `123`
- 修改密码 / 修改个人信息：前端提交更新接口完成

> Go 认证机制：密码使用 bcrypt 哈希存储；登录成功后签发 JWT（`golang-jwt`，载荷含用户 id 与角色，HS256 签名，2 小时过期）。每次受保护请求由 **JWT 中间件**解析 `token`（请求头或查询参数），验签通过后将用户上下文写入 `gin.Context`；失败返回 401，移动端收到 401 自动跳登录页。修改密码后可令旧 token 失效（如启用 token 版本号纳入签名/会话表校验）。

**（2）统一响应体**：`{code, msg, data}`，200 成功 / 401 认证失败 / 500 业务或系统错误。Go 中实现统一的 `Response` 结构体与 `c.JSON()` 封装 + Recovery 中间件兜底 panic。

**（3）文件上传下载**（`/file`）
- 通用上传（`/file/upload`）：`c.FormFile("file")`，单文件最大 25MB，存储到服务器的 `files/` 目录（gin 中间件限制上传大小），重名自动加时间戳前缀，返回可直接访问的下载/预览链接（URL 用配置文件中 `ip:port` 拼装）
- 下载（`/file/download/{name}`，`c.FileAttachment` 附件方式）、预览（`/file/yulan/{name}`，`c.File` 内联展示图片/视频）
- 富文本上传（`/file/editor/upload`）：支持 type=img / type=video，返回 wangeditor 兼容格式 `{errno, data:{url}}`

**（4）操作日志（中间件）**
- Go 中通过 **Gin 中间件** 实现（替代 Java AOP）：受保护路由组按操作的 module/type 打标（如 `handler.SetOperation(ctx, "订单", "新增")` 或每个 handler 声明操作元数据），中间件在 handler 执行成功后**异步**（goroutine）写入 logs 表
- 日志字段：操作模块、操作类型（新增/修改/删除/批量删除/登录/注册）、操作人 IP（优先 `c.ClientIP()`，或解析 `X-Forwarded-For`/`X-Real-IP`）、操作人（从 JWT 上下文取）、时间
- 管理后台可查询、删除、批量删除日志

**（5）跨域支持**：`gin-contrib/cors` 全局放行 + 管理后台静态部署时也常通过同源/反向代理规避。

---

### 4.2 移动端 —— 用户（消费者）功能

**首页（index）**
- 搜索栏（展示，搜索逻辑预留）
- 轮播图（swiper 自动轮播，数据源 `/banners/selectAll`，后端未实现对应接口，当前为预留）
- 公告栏（喇叭图标 + 文字多条轮播，每 3 秒切换，数据 `/notice/selectAllApp`）
- 商品分类入口（主食 / 饮品 / 水果 / 小食，静态展示）
- 热门商家列表（Logo、名称、评分、销量、配送信息、免费配送标签），点击进入商家详情

**商家详情页（detail）**
- 商家信息区：名称、收藏星标（可收藏/取消）、配送标签、地址、电话、Logo
- "全部商品 / 评价" 双标签切换
- 商品区：左侧商品分类导航 + 右侧商品列表（图片、名称、描述、折扣标签、已售、原价划线、到手价、选购按钮）
- 评价区：用户头像、昵称、评价内容、星级评分、时间
- 底部购物车栏：购物车图标 + 数量角标 + "立即下单"按钮；弹出层查看购物车（商品列表、数量加减、清空、总金额）

后端支撑接口：`/business/selectById`、`/category/selectAllByBid`、`/product/selectAllByCid`、`/comment/selectAllByBid`、`/collect/selectByUidBid`、`/collect/add`、`/collect/update`、`/cart/add`、`/cart/selectAll/{bid}/{uid}`、`/cart/calc`、`/cart/update`、`/cart/deleteByBid/{bid}/{uid}`

**商品搜索页（productPage）**：按商品名模糊搜索商品列表，展示折扣价（数据 `/product/selectByApp`，后端未实现，当前为预留）。

**购物车核心逻辑（后端 /cart）**
- 加购：同一商家同一商品数量累加
- 算价：数量 × 商品实际价之和（decimal 保留两位小数）
- 查询：GORM `Preload` 拼装完整商品与商家信息
- 清除：下单成功后自动清空该用户在某商家的购物车

**确认订单页（confirm）**
- 收货地址区（未选时提示"请添加收货地址"，点击跳地址选择）
- 商品清单（图片、名称、数量、原价划线、实际价）
- 费用明细：打包费、配送费（划线免费）、店铺活动优惠、满减红包、已优惠合计、小计/合计
- 商家承诺展示（慢必赔、隐私保护、食品封签）、备注入口、餐具份数、发票
- 支付方式选择（支付宝 / 微信，默认支付宝）
- 提交订单 → 调用 `/orders/addOrder`

**订单列表页（orders）**：顶部"全部 / 进行中 / 待评价 / 退款"标签，订单卡片按状态显示不同操作按钮：
- 待支付 → 去支付（状态更新为"待发货"，后端自动记录支付时间）
- 待发货 → 在线联系、取消订单（→ 已取消）
- 配送中 → 确认收货（→ 已完成）
- 已完成未评价 → 去评价、再来一单（商品重新加入购物车）
- 已完成已评价 → 再来一单

**订单详情页（ordersItem）**：商品明细、优惠与实付、收货信息、备注、发票说明、订单编号、下单/支付时间、配送方式、支付方式。

**地址管理（address / addAddress）**
- 地址列表：新增、编辑、删除，点击某条选中并返回确认订单页
- 新增/编辑表单：详细地址、联系人、联系电话（必填校验）
- 后端支撑：`/address/selectAll/{uid}`、`/address/selectById`、`/address/add`、`/address/update`、`/address/delete/{id}`

**收藏（collect）**：收藏的商家列表，点击进入商家详情。后端支撑 `/collect/selectByUid/{uid}`。

**订单评价（comment / MyComment）**
- 评价：评价内容 + 星级，提交后把订单 `commentStatus` 置为"已评价"
- 我的评论：本人所有评论列表（商家名、内容、星级、时间）

**个人中心（me）**
- 用户信息展示（头像、账号、姓名）
- 功能入口：收货地址、我的收藏、我的评论、个人信息、修改密码、关于我们、用户协议、联系客服、实名认证、退出登录
- 实名认证用户额外显示"外卖接单"入口

**个人信息（person）**：头像上传（`/file/upload`）、账号只读、姓名、性别、联系电话、邮箱，保存后同步本地缓存。

**修改密码（editPassword）**：旧密码 / 新密码 / 确认密码，前端校验旧密码一致后提交（后端 bcrypt 校验并更新）。

**关于我们 / 用户协议**：静态内容页。

---

### 4.3 移动端 —— 骑手（配送员）功能

**实名认证（orderTake）**：填写真实姓名、身份证号（18 位校验）、学号、联系电话（11 位校验），提交 `/deliveryman/add`（预留接入点）。注册成功后将用户标记为已认证骑手，"我的"页面显示接单入口。

**外卖接单（takeoutOrder）**
- 展示待接订单列表：订单编号、店铺名称、状态"已出餐"、收货人、手机号、收货地址、备注
- 点击"接单"：订单状态更新为"正在配送"，绑定骑手 id / 姓名 / 电话（可在同一事务内校验并发：使用条件更新 `UPDATE orders SET status='正在配送', did=? WHERE id=? AND status='已出餐'`，影响行数为 0 则说明已被抢单）
- 后端支撑：`/orders/selectTakeout`（预留接入点）、`/orders/update`

镜像到完整外卖流程：**用户下单 → 待支付 → 支付 → 待发货 → 商家出餐 → 已发货 → 骑手接单 → 正在配送 → 用户确认收货 → 已完成 → 评价**；分支：取消（→ 已取消）、退款（→ 已退款）。

---

### 4.4 PC 管理后台功能

**登录注册（Login / Register）**
- 登录：账号 + 密码 + 前端生成图形验证码（4 位、随机颜色/旋转，点击刷新，大小写不敏感校验）
- 忘记密码：用户名 + 手机号弹窗重置，重置后密码为 `123`
- 成功登录后用户数据（含 token）存入 `localStorage`（key：`honey-user`）

**系统首页（Home）**：欢迎卡片 + 系统公告时间线 + 新闻时间线。

**用户管理（User）**
- 列表分页 + 按用户名/姓名模糊搜索
- 新增 / 编辑 / 删除 / 批量删除
- Excel 批量导入、批量导出（支持按搜索条件或按勾选行导出）
- 头像上传

**管理员管理（Admin）**：与用户管理基本一致，管理角色为"管理员"的账号。（注意：现有代码中导入按钮误指向 `/user/import`，重建时应收敛到 `/admin/import`。）

**商家管理（Business）**
- 商家列表（名称、电话、简介、地址、Logo、所属店主、审核状态）
- 新增 / 编辑（含审核状态"通过"/不通过）/ 删除 / 批量删除
- Logo 上传

**商品管理（Product）**
- 商品列表（名称、价格、数量、描述、图片、所属商家、所属分类）
- 新增（名称、价格、数量、描述、分类、图片上传）/ 编辑 / 删除 / 批量删除

**商品分类管理（Category）**：分类名称增删改查，关联所属商家。

**新闻管理（News）**
- 富文本编辑（wangeditor，支持图片/视频上传，代码高亮）
- 列表分页 + 标题搜索、新增、编辑、删除、批量删除
- 内容预览弹窗、前台详情页跳转展示

**系统公告管理（Notice）**：标题 + 富文本内容，增删改查，Switch 一键切换"是否公开"（移动端首页公告栏取已发布公告）。

**系统日志（Logs）**：按模块/类型/操作人查询，删除、批量删除（日志由后端中间件自动生成）。

**数据统计（Charts）**：ECharts 折线图（订单销售趋势）、柱状图（分类销售）、饼图（销售占比）。（对应后端 `/charts` 接口需由 Go 实现统计聚合，可用单条 SQL `GROUP BY date/分类` 或定时任务预聚合。）

---

## 5. 核心业务流程

### 5.1 点餐下单主流程

```
浏览首页 → 进入商家详情 → 按分类选购商品加入购物车（同商品自动累加）
   → 购物车弹出层核对/调整数量 → 点击"立即下单"
   → 确认订单页：选择/添加收货地址 → 填写备注 → 选择支付方式
   → 提交订单（POST /orders/addOrder）
      · 校验登录与购物车非空
      · 计算实付金额（商品7折总计，decimal）
      · 生成订单主表（状态=待支付、雪花订单号、封面图、商品概要）
      · 逐条生成订单明细（商品名/图/单价/数量快照）
      · 清空该商家购物车
   → 订单列表"去支付" → 状态更新为"待发货"（自动记录 payTime）
   → 待收货 / 配送中 → 用户"确认收货" → 已完成
   → 去评价 → 订单 commentStatus=1
```

> Go 实现要点：`orders.addOrder` 全程在 **GORM 事务**内执行（`tx.Transaction(func(tx *gorm.DB) error {...})`），任一环节失败自动回滚，保证订单、明细、清购物车三者一致。必要时对"同用户下单""骑手抢单"等场景用数据库行/条件更新保证并发安全。

### 5.2 订单状态流转

| 状态名 | 含义 | 触发方/操作 |
|---|---|---|
| 待支付 | 初始状态 | 下单生成 |
| 待发货 | 已支付 | 用户"去支付"（记录支付时间） |
| 待收货 | 商家已出餐/已发货 | 商家发货（后端支持状态更新） |
| 配送中（移动端） | 骑手配送 | 骑手接单，绑定骑手信息 |
| 已完成 | 交易完成 | 用户"确认收货" |
| 已取消 | 取消订单 | 用户"取消订单" |
| 已退款 | 退款 | 退款流转 |

按用户状态分组查询：`进行中` = 待支付 ∪ 待发货 ∪ 待收货；`待评价` = 已完成且未评论；`退款` = 已退款；其余 = 全部。建议在 Go 中定义 `OrderStatus` 常量（iota + String），状态迁移集中收敛到 service 校验合法跳转。

### 5.3 7 折优惠计算

全系统统一定价规则：**商品实际价 = 原价 × 0.7**（`shopspring/decimal`，保留两位小数四舍五入，替代 Java 的 BigDecimal）。
- `Product.realPrice`（商品实付单价）、购物车金额汇总（`/cart/calc`）、订单实付金额（`/orders/addOrder`）、订单明细单价均按此规则计算。
- 优惠金额 = 原价合计 − 实付合计，通过金额模型 `AmountDTO{amount 总额, discount 优惠额, actual 实付额}` 表达。
- 建议在 Go 中抽出 `PriceService`，所有取价入口统一走该服务，保证多端口径一致。

---

## 6. 数据库表结构

以下为主要表及字段（主键均为自增整数）。GORM 通过 struct tag 映射表/列名，`AutoMigrate` 可自动建表。

### user（用户）
id、username（用户名）、password（密码哈希）、name（姓名）、phone（手机号）、email（邮箱）、address（地址）、avatar（头像URL）、role（角色：管理员/用户/商家等）、sex（性别）。登录后附加 token（非表字段）。

### business（商家）
id、name（店铺名）、phone、introduce（介绍）、logo、address、status（审核状态：通过/其它）、uid（关联 user.id 店主）。附加 username（店主姓名，非表字段）。

### category（商品分类）
id、name、bid（所属商家 id）、bname（商家名）。

### product（商品）
id、name、description、price（原价）、sum（数量/销量）、picture、bid（商家 id）、cid（分类 id）。附加 realPrice（实际价）、business/category（名称，非表字段）。

### cart（购物车）
id、pid（商品 id）、num（数量）、uid（用户 id）、bid（商家 id）。附加 product/business 对象。

### orders（订单主表）
id、orderNo（雪花订单号）、payType（支付方式）、payTime（支付时间）、status（状态）、bid（商家 id）、user（收货人）、phone（电话）、addressId（地址 id）、uid（用户 id）、amount（总金额）、actual（实付金额）、comment（备注）、cover（封面图）、name（概要，如"奶茶等3件商品"）、time（下单时间）、commentStatus（0未评/1已评）。附加 business/address 对象、pid 列表。（骑手接单场景建议追加：did 骑手id、deliverymanName、deliverymanPhone。）

### ordersitem（订单明细）
id、orderId、num、price（单价）、productName（商品名快照）、productPicture（图片快照）。附加 realPrice。

### address（收货地址）
id、address、user（收货人）、phone、userId（所属用户 id）。

### comment（评论）
id、star（星级）、content、time、orderId、uid、bid。附加 business/user 对象。

### collect（收藏）
id、businessName、bid、uid、time、isCollect（1收藏/0取消）。附加 business 对象。

### notice（公告）
id、title、content、userid（发布人 id）、time、open（是否公开）。附加 user 姓名。

### news（新闻）
id、title、description（摘要）、content、authorid、time。附加 author 姓名。

### logs（操作日志）
id、operation（模块）、type（类型）、ip、user（操作人）、time。

---

## 7. 后端接口清单（Go 实现）

> 除标注【公开】外，其余接口均需请求头携带 `token`（JWT 中间件校验）。路由注册建议：公开组放行；其余业务路由全挂在鉴权分组下。

### 7.1 认证【公开】
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /login | 登录，返回用户 + token |
| POST | /register | 管理后台注册 |
| POST | /appRegister | 移动端注册（角色=用户） |
| PUT | /password | 忘记密码重置（用户名+手机号 → 123） |

### 7.2 用户 /user
add、update、delete/{id}、delete/batch、selectAll、selectById/{id}、selectByPage、export（Excel导出，excelize）、import（Excel批量导入）

### 7.3 管理员 /admin
同 `/user` 结构（add、update、delete、delete/batch、selectAll、selectById、selectByPage、export、import）。删除/批量删除时**禁止删除当前登录账号**。

### 7.4 商家 /business
add（自动置 status=通过、uid=当前用户）、update（含审核状态）、delete/{id}、delete/batch、selectAll、selectAllApp（仅审核通过，供 App 首页）、selectById/{id}、selectByPage

### 7.5 分类 /category
add（自动关联当前用户的商家）、update、delete/{id}、delete/batch、selectAll、selectAllByBid/{bid}、selectById/{id}、selectByPage

### 7.6 商品 /product
add（自动关联商家、按分类名填 cid）、update、delete/{id}、delete/batch、selectAll、selectAllByCid/{cid}（含 realPrice）、selectByPage（拼装商名/分类名）

### 7.7 购物车 /cart
calc（算总金额）、add（同商品数量累加）、update、delete/{id}、deleteByBid/{bid}/{uid}（清空某商家购物车）、delete/batch、selectAll/{bid}/{uid}、selectAllApp（按当前登录用户）、selectById/{id}、selectByPage

### 7.8 订单 /orders
addOrder（购物车→订单+明细+清空购物车，GORM 事务）、add、update（状态改为"待发货"时自动记录 payTime）、delete/{id}、delete/batch、selectAll、selectOrders/{uid}/{status}（按状态分组）、selectById/{id}（拼装商家与地址）

### 7.9 订单明细 /orderItem
add、update、delete/{id}、delete/batch、selectAll、selectByOrderId/{orderId}（含 realPrice）、selectById/{id}

### 7.10 地址 /address
add（自动填 userId=当前用户）、update、delete/{id}、delete/batch、selectAll/{userId}、selectById/{id}、selectByPage

### 7.11 评论 /comment
add（自动填时间与订单的 bid/uid，并置订单已评价）、update、delete/{id}、delete/batch、selectAll、selectAllByUid/{uid}、selectAllByBid/{bid}、selectById/{id}、selectByPage

### 7.12 收藏 /collect
add（收藏置 1）、update（取消收藏置 0）、delete/{id}、delete/batch、selectAll、selectByUid/{uid}（查询收藏中的商家）、selectByUidBid/{uid}/{bid}（是否已收藏）、selectByPage

### 7.13 公告 /notice
add（自动填发布人与时间）、update、delete/{id}、delete/batch、selectAll、selectAllApp（仅已发布）、selectById/{id}、selectByPage、selectUserData（首页公告栏数据）

### 7.14 新闻 /news
add（自动填作者与时间）、update、delete/{id}、delete/batch、selectAll、selectById/{id}、selectNewsData（最新两条）、selectByPage

### 7.15 日志 /logs
delete/{id}、delete/batch、selectByPage

### 7.16 文件 /file
upload【公开】、download/{name}【公开】、yulan/{name}【公开】、editor/upload（富文本图片/视频）

### 7.17 预留接口（前端已调用，Go 后端需补齐实现）
| 接口 | 说明 | 前端使用位置 |
|---|---|---|
| GET /banners/selectAll | 首页轮播图列表（banners 表） | index 首页 |
| POST /deliveryman/add | 骑手实名认证注册（生成骑手记录） | orderTake 实名认证 |
| GET /orders/selectTakeout | 待接单订单列表（状态=已出餐/已发货） | takeoutOrder 接单 |
| GET /product/selectByApp?name= | 商品模糊搜索 | productPage 搜索 |
| PUT /user/updatePassword | 独立改密接口（bcrypt 校验旧密码） | editPassword 修改密码 |
| GET /charts | 数据统计数据聚合 | charts 数据统计 |

---

## 8. 配置示例（Go 后端 config.yaml）

```yaml
server:
  port: 9090            # 服务端口

ip: localhost           # 文件访问链接拼接用域名/IP

database:
  driver: mysql
  dsn: "root:password@tcp(127.0.0.1:3306)/honey2024?charset=utf8mb4&parseTime=True&loc=Local"
  max-idle: 10
  max-open: 100

jwt:
  secret: "your-secret-key"   # JWT 签名密钥
  expire-hours: 2

upload:
  dir: "./files"             # 上传文件根目录，与 Gin 静态路由 /file 对应
  max-size: 25               # 单文件最大 MB

log:
  level: info
```

---

## 9. 说明事项（Go 重建项目时注意）

1. **三端职责边界**：后端提供全部业务能力；PC 后台面向管理员/商家；移动端面向用户/骑手。三方通过 token 区分身份，token 存于各端本地缓存（后台 `honey-user`，移动端 `ccdx-user`）。前端请求封装不变，仅需确保 baseURL 指向新 Go 服务地址。
2. **订单状态耦合点**：支付动作 = 订单状态由"待支付"改为"待发货"（后端在该时机写 `payTime`）；评价完成 = 订单 `commentStatus` 置 1。这两处是订单状态机的"约定动作"，Go 重建时建议封装为显式 service 方法（`Pay()/Complete()/Cancel()/Comment()/Take(order)`），并在单文件或常量中集中定义合法状态迁移。
3. **金额规则单一来源**：7 折实际价计算集中在 Go 的 `PriceService`（基于 `shopspring/decimal`）。所有接口统一从这里取价，前端只做展示（原价划线 + 到手价 + 已优惠合计），避免多端重复计算造成口径不一致。所有金额字段类型建议用 `decimal.Decimal` 或 `int64`（分）存储。
4. **并发与事务**：下单（订单+明细+清购物车）与骑手抢单必须在事务中完成；抢单使用条件更新（`WHERE status='已出餐'`）防重复接单；金额入库前用 decimal 校验避免浮点误差。
5. **预留/未完成功能**：移动端搜索、轮播图、实名认证、骑手接单等入口已成型但原后端未实现，Go 重建时可直接补齐（见 7.17 接口清单）；PC 后台"购物车管理""品牌管理"为占位/半成品页面，可裁剪。
6. **已知缺陷可优化**：管理员批量导入误指向 `/user/import`；侧边栏"订单管理"无对应路由会 404；前端路由守卫未启用。重建时应统一收口。
7. **部署要点**：Go 后端编译为单一二进制，轻量易部署（Linux/Windows 交叉编译）；端口 9090 与前端 baseURL 保持一致；文件目录 `./files` 挂载静态资源；数据库 utf8mb4、时区 GMT+8；生产环境 JWT 密钥与数据库密码通过环境变量注入，勿硬编码。

---

## 10. 功能审视与优化建议（重建参考）

> 审视维度：安全、订单/支付正确性、商家与骑手业务闭环、运营能力、平台管理、工程质量。优先级：**P0 必修**（安全与正确性）、**P1 建议**（业务闭环/运营）、**P2 可选**（增强体验）。

### 10.1 安全与权限（P0）

| # | 问题（现状基线） | 影响 | 优化建议 |
|---|---|---|---|
| 1 | 忘记/重置密码固定重置为 `123` | 弱口令、账号易被接管 | 改为：随机一次性临时密码下发（短信/邮件）或短信验证码 + 用户自设新密码；重置接口限频 |
| 2 | JWT 以"用户密码"作为 HMAC 密钥 | 密钥可被推断、改密即全量失效、密钥不可控 | 改为固定服务端 `jwt.secret` + 载荷含 `uid/role/tokenVersion`；改密/踢人靠递增 tokenVersion 使旧 token 失效 |
| 3 | 登录无失败次数限制、后端无验证码（验证码仅前端） | 可被暴力破解 | 登录失败锁定（5 次锁 15 分钟）+ 后端下发图形/滑块验证码；同 IP/账号限频 |
| 4 | 资源接口仅凭请求参数 uid/bid 操作，未校验归属 | **IDOR 越权**：可删/改他人的地址、购物车、订单、评论 | 服务层统一校验"资源 owner == 当前登录 uid"；订单/地址等按 uid 全部从 token 上下文取值，不信任客户端传参 |
| 5 | 商家可改/删任意商品与分类，无数据范围限制 | 商家越权操作他人店铺 | 商家分组接口强制 `bid == 当前用户商家`；管理员分组才可全局操作 |
| 6 | 文件上传仅限 25MB，无类型白名单 | 可上传 html/木马等恶意文件 | 扩展名 + MIME 双白名单（jpg/png/gif/webp/mp4），图片压缩/重编码，文件另存随机名，禁止执行目录权限；限制每日上传次数 |
| 7 | 管理后台路由守卫被注释、角色仅靠菜单显隐 | 前端可绕过看到/操作受限页面 | 启用前端路由守卫 + 后端每接口角色校验（管理员/商家分组中间件），双保险 |
| 8 | 密码以明文逻辑存储提及不足 | 撞库泄露 | 全线 bcrypt（登录/注册/改密/重置），日志、响应中不回传 password |

### 10.2 订单与支付（P0/P1）

| # | 问题 | 影响 | 优化建议 |
|---|---|---|---|
| 1 | "支付"仅为改状态（待支付→待发货） | 无真实支付，无回调、无对账 | Go 版建议引入支付单：`orders` + `payments`（支付单）双表；对接支付宝/微信或先做**模拟支付网关**（下单→创建支付单→模拟回调→异步更新订单）。回调配幂等键防重复入账 |
| 2 | 无超时未支付自动取消 | 僵尸订单占用库存 | cron 定时任务扫描超时（如 30 分钟）未付订单 → 自动取消 + 释放库存 |
| 3 | 无重复支付/重复操作防护 | 用户连点"去支付"可重复记录 payTime | 状态机前置校验：仅"待支付"可支付、仅"待发货"可取消、仅"待收货"可确认收货，非法迁移直接拒绝（条件更新 `WHERE status=?` 影响行数判断） |
| 4 | 下单不校验库存、不扣减、无超卖防护 | 超卖、负库存 | product 增加 `stock` 字段；下单事务内 `UPDATE product SET stock=stock-?, sales=sales+? WHERE id=? AND stock>=?`；失败回滚并提示库存不足 |
| 5 | 无退款流程 | "已退款"不可达/不可操作 | 退款申请（用户，附原因）→ 商家/管理员审核 → 退款单 + 状态流转；记录退还款与时间 |

### 10.3 费用与优惠（P1）

| # | 问题 | 影响 | 优化建议 |
|---|---|---|---|
| 1 | 打包费/配送费/店铺活动/满减红包在前端写死 | 口径不一致、无法运营 | 费用计算全部下沉后端：`cart/calc` 与 `orders/addOrder` 内按统一"费用明细"结构（打包费、配送费、起送价、活动优惠、实付）返回，所有端展示后端结果 |
| 2 | 无优惠券/满减体系 | 无法营销 | 增加 `coupon`（券模板/领券/核销）与订单 `couponId` 绑定；订单实付 = 商品折后 − 优惠券（可选 P2，先支持满减规则表） |
| 3 | 金额用 double 双精度 | 累计误差 | 数据库用 `DECIMAL`，Go 侧全部 `shopspring/decimal`，接口返回格式化两位小数 |

### 10.4 商家端（P1）

| # | 问题 | 影响 | 优化建议 |
|---|---|---|---|
| 1 | 无独立商家工作台/出餐操作 | 商家无法推进订单（待发货→已发货） | PC 后台增加"商家工作台"：本人店铺、本人的商品/分类、待出餐订单、"出餐/发货"按钮；所有数据按商家隔离 |
| 2 | 商家审核仅"通过/不通过"两态 | 无法反馈驳回原因 | 审核增加 `reason（驳回原因）、reviewer、reviewTime`；审核结果通知商家 |
| 3 | 无营业状态、起送价、营业时间、配送范围 | 打烊仍可下单 | business 增加：`openStatus（营业/打烊）、minAmount（起送价）、openTime/closeTime、deliveryRadius`；下单前校验 |
| 4 | 入驻审核与"uid=当前用户"绑定过于简单 | 一个账号多店铺、店主信息弱 | 增加入驻申请流程（申请表 + 资质字段），审核通过才成为商家角色 |

### 10.5 商品与展示（P1）

| # | 问题 | 影响 | 优化建议 |
|---|---|---|---|
| 1 | 无上下架状态 | 下架商品仍可购买 | product 增加 `isActive`；列表/详情仅展示在架，加购/下单再校验 |
| 2 | `sum` 语义混用（数量/销量） | 数据不准确 | 拆分为 `stock`（库存）与 `sales`（销量）；下单扣库存增销量 |
| 3 | 评分、销量、配送时间是前端写死的"4.5 分/500 单/30 分钟" | 展示虚假、无运营参考 | 评分 = 评论实时/定时聚合（`AVG(comment.star)`）；销量 = `product.sales` 聚合；配送时长 = 系统预估或商家配置 |
| 4 | 多图、规格（SKU）缺失 | 无法表达多样化商品 | product 支持多图（图序数组/子表）；复杂商品可建 `sku` 子表（规格、价格、库存）（P2） |

### 10.6 配送（P1）

| # | 问题 | 影响 | 优化建议 |
|---|---|---|---|
| 1 | 配送费固定 2.5 且免费规则前端写死 | 无法按距离/金额运营 | 后端配送费规则：按距离/金额门槛减免，随费用明细返回 |
| 2 | 骑手订单只有"接单→配送中"，无送达环节 | 订单无法闭环到"已送达" | 状态细化：待取餐→配送中→**已送达**；送达由骑手确认（需用户签收码可选）；模块化实现 |
| 3 | 实名认证接口无后端支撑（预留） | 骑手无法真正注册 | Go 版补齐：`deliveryman` 表 + 审核 + 与 `user.realStatus` 联动，写入上文状态闭环 |
| 4 | 无配送轨迹/定位/ETA | 体验弱 | 可选接入地图 SDK：商家/用户定位、配送范围判定、预计送达时间、骑手位置轨迹（P2） |
| 5 | 无骑手结算 | 骑手激励缺失 | 配送订单生成骑手收益（按单计费），账单/提现（P2） |

### 10.7 评价（P1/P2）
- 商家可**回复**评价（comment 增加 `reply` 或独立 reply 表；回复后用户可见）。
- 评价支持**图片**（可选，files 复用）与**追评**（P2）。
- 评分与商家、商品详情联动展示真实均分（见 10.5-3）。

### 10.8 首页与运营（P1/P2）

| # | 问题 | 影响 | 优化建议 |
|---|---|---|---|
| 1 | 轮播图无后端实现（预留） | 首页关键运营位空白 | 补 `banners` 表 + PC 后台 CRUD + 上架开关 + 排序，App 首页读取 |
| 2 | 搜索功能前端预留未实现 | 用户无法检索 | 补 `/product/search`、`/business/search`；支持搜索历史、热门词（P2 加 Redis 词频） |
| 3 | 分类入口（主食/饮品/水果/小食）是静态死链 | 无法落地 | 分类入口跳转"分类商品页"（按分类/商家过滤），或改为瀑布流 |
| 4 | 热门商家无排序规则 | 无法运营 | 排序支持：综合（评分×销量）、销量、评分、距离、新店 |
| 5 | "联系客服"为空链接、"关于我们"静态 | 售后无入口 | 客服落地：在线工单/表单或企业微信二维码；FAQ 页 |
| 6 | 无消息/通知中心 | 状态变化无触达 | `notification` 表：订单状态变更、活动公告生成消息；App 消息中心 + 可选推送（UniPush）（P2） |

### 10.9 平台管理（P1）

| # | 问题 | 影响 | 优化建议 |
|---|---|---|---|
| 1 | 无订单管理后台页（路由 404） | 管理员无法兜底处理订单 | 新增订单管理页：全量订单 + 商家/状态/时间筛选 + 查看明细 + 退款处理 |
| 2 | 无数据仪表盘 | 首页缺乏经营指标 | 首页仪表盘卡片：用户数、商家数、订单数、销售额、今日单量，实时统计 |
| 3 | `/charts` 统计未实现 | 数据统计页空 | Go 实现聚合：日销售趋势、分类销售、Top 商品/商家；可选定时预聚合表 + Redis 缓存 |
| 4 | 日志无导出 | 审计不便 | logs 增加 Excel/CSV 导出 |
| 5 | Excel 导入无模板与反馈 | 用户难用、出错不可见 | 提供模板下载；导入返回每行成功/失败原因（失败行明细） |

### 10.10 工程质量（P0/P1）

| # | 问题 | 影响 | 优化建议 |
|---|---|---|---|
| 1 | 无软删除、无审计字段 | 误删不可恢复 | 所有业务表 GORM 软删除（gorm.DeletedAt）+ `created_at/updated_at，create_by/update_by` |
| 2 | 分页参数、pageSize 各页不一致 | 交互不统一 | 统一默认 10、上限 100；统一 `PageResult{records,total}` |
| 3 | 无错误码规范 | 前后端判断混乱 | 统一错误码表（如 10000 起按模块分段），前端按 code 处理 |
| 4 | 无接口文档与测试 | 联调/回归困难 | Go 用 swag/OpenAPI 自动生成接口文档；对订单/金额/权限核心逻辑写单元测试 |
| 5 | 多端金额/状态口径不统一 | 展示与结算不一致 | 状态枚举、状态机迁移、金额计算只收敛在后端 service（DRY），前端仅渲染 |
| 6 | 配置/密钥硬编码风险 | 泄密 | config.yaml + 环境变量覆盖；生产密钥轮换机制 |

### 10.11 体验增强（P2，可选）
- 移动端：下拉刷新、空态/加载骨架、图片懒加载与压缩、令牌自动续期（refresh token）。
- 登录：手机号验证码登录、微信一键登录（UniApp 条件编译）。
- 多端缓存一致性：App 端购物车支持未登录状态本地暂存，登录后合并。

---

## 11. 采纳建议后的目标形态（概要）

若全部采纳 P0/P1，系统将升级为具备**安全访问控制、真实支付模拟、库存闭环、商家工作台、骑手配送闭环、运营内容管理、经营分析**的完整外卖平台；P2 按预算取舍。表结构在上述 6 章基础上扩展：`banners`、`payments`（支付单）、`refunds`（退款）、`coupons`（优惠券）、`deliveryman`（骑手）、`notification`（消息）、`sku`（规格）、软删除与审计字段等。功能基线 + 本章建议即构成"用 Go 重建"的完整需求边界。