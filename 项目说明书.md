# 校园外卖点餐平台 — 项目说明书（简易版）

## 一、项目简介

面向校园的在线点餐与配送平台，含三条业务线：**用户点餐、商家经营、平台管理**。

| 端 | 目录 | 技术栈 |
|---|---|---|
| 后端服务 | `server/` | Go 1.25 · Gin · GORM · SQLite（可切 MySQL） |
| PC 管理后台 | `admin/` | Vue 3 · Vite · Element Plus · Pinia · ECharts |
| 微信小程序（用户端） | `miniprogram/` | 原生小程序 · @vant/weapp · @vant/icons |

- 后端接口：131 个，统一响应体 `{code, msg, data}`
- 数据表：15 张，117 个字段（详见 `数据库字典.md`）
- 小程序：19 个页面

## 二、快速开始

### 1. 启动后端

```bash
cd server
go mod tidy
go run .          # 监听 :9090，首次启动自动建表并写入种子数据
```

### 2. 启动管理后台

```bash
cd admin
npm ci
npm run dev       # http://localhost:5173
```

### 3. 运行小程序

微信开发者工具 → 导入项目 → 目录选**仓库根目录**（含 `project.config.json`）
→ 详情 → 本地设置 → 勾选「**不校验合法域名**」（本地后端是 http）。
后端地址在 `miniprogram/app.js` 的 `globalData.baseUrl` 中配置。

### 4. 测试账号

| 角色 | 账号 | 密码 |
|---|---|---|
| 管理员 | `admin` | `admin123` |
| 普通用户 | `test` | `123456` |
| 商家店主 | `shop` | `123456` |

## 三、主要功能

**用户端（小程序）**：首页浏览 → 商家详情选购 → 购物车 → 确认订单（地址/备注/支付方式）
→ 下单 → 支付 → 确认收货 → 评价；另有商品搜索、收藏、地址管理、个人信息、修改密码。

**管理后台**：数据统计、用户/管理员/商家/商品/分类/订单/评论/公告/新闻/轮播图/日志管理，
支持分页查询、Excel 导入导出、图片上传。

**核心业务规则**：

- **7 折优惠**：实付 = 原价 × 0.7（后端 `shopspring/decimal` 计算，保留两位小数）
- **下单事务**：购物车 → 订单主表 + 明细 → 清空该商家购物车，全程在一个事务内
- **订单状态流转**：待支付 → 待发货 → 待收货 → 已完成；分支：已取消 / 已退款
- **认证**：JWT（HS256，2 小时），密码 bcrypt 存储；改密后 `token_version` 自增使旧 token 失效
- **操作日志**：中间件在业务成功后异步写入 `logs` 表

## 四、工程结构

```
code/
├── server/                 # Go 后端
│   ├── main.go             # 入口
│   ├── config.yaml         # 端口 / 数据库 / JWT / 上传配置
│   └── pkg/
│       ├── config/         # 配置加载
│       ├── core/           # 统一响应体、分页
│       ├── models/         # GORM 模型 + 建表 + 种子数据
│       ├── handler/        # 各业务接口
│       ├── middleware/     # JWT / CORS / Recovery / 操作日志
│       ├── router/         # 路由注册（公开组 + 鉴权组）
│       └── utils/          # JWT / 雪花 ID / 密码 / Excel / 金额
├── admin/                  # Vue 管理后台
├── miniprogram/            # 微信小程序（用户端）
├── 功能说明书.md            # 需求文档
├── 数据库字典.md            # 数据表结构
└── miniprogram/DESIGN.md   # 小程序设计系统
```

## 五、配置说明

`server/config.yaml`：

```yaml
server:
  port: 9090
ip: localhost                 # 文件访问链接拼接用
database:
  driver: sqlite              # sqlite | mysql
  dsn: "./data/honey2024.db"
jwt:
  secret: "..."               # 生产环境请通过环境变量 JWT_SECRET 注入
  expire-hours: 2
upload:
  dir: "./files"
  max-size: 25                # MB
```

## 六、验证工具

项目自带三套校验，可在无图形环境验证：

```bash
python3 miniprogram/tools/check.py          # 小程序静态校验（结构/依赖/图标名/WXML 表达式）
node miniprogram/tools/verify.js            # 小程序联调（真实请求打到后端，65 项）
node admin/tools/verify-table-render.cjs    # 管理后台表格字段渲染（14 项）
python3 api_smoke_test.py                   # 后端接口冒烟测试（72 项）
```

## 七、注意事项

1. **默认分支**：代码在 `master` 分支。克隆时请用 `git clone --branch master`，
   或将仓库默认分支改为 `master`，否则会拿到空的 `main` 分支。
2. **小程序字体**：`@vant/icons` 官方样式指向 `at.alicdn.com` 外链，小程序真机不可用；
   本项目已将 woff2 以 base64 内嵌到 `miniprogram/vant/icon/index.wxss`。
3. **组件引入**：Vant 组件已按依赖闭包放入 `miniprogram/vant/`，使用相对路径引用，
   **无需**在开发者工具中执行「构建 npm」。
4. **生产部署**：小程序需将后端换为已备案的 HTTPS 域名并加入 request 合法域名；
   后端密钥、数据库密码应通过环境变量注入。
