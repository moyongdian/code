# 数据库字典（简易版）

- 数据库：SQLite（默认，`server/data/honey2024.db`）／可切换 MySQL
- 建表方式：GORM `AutoMigrate` 自动建表（`server/pkg/models/models.go`）
- 共 **15 张表 / 117 个字段**，主键均为自增整数 `INTEGER`
- 时间字段统一为字符串，格式 `2006-01-02 15:04:05`
- 金额字段为 `REAL`，业务计算保留两位小数

> 命名说明：GORM 默认将驼峰字段转为下划线列名，因此 `UserID` → `user_id`、
> `BID` → `bid`、`ProductPicture` → `product_picture`。

---

## 1. users — 用户

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| username | TEXT | 用户名（**唯一索引** `idx_users_username`） |
| password | TEXT | bcrypt 哈希 |
| name | TEXT | 姓名 |
| phone | TEXT | 手机号 |
| email | TEXT | 邮箱 |
| address | TEXT | 地址 |
| avatar | TEXT | 头像 URL |
| role | TEXT | 角色：管理员 / 用户 |
| sex | INTEGER | 性别：0 女 1 男 |
| real_status | BOOLEAN | 是否实名认证（骑手），默认 false |
| token_version | INTEGER | JWT 令牌版本，改密后自增使旧 token 失效 |

> 登录成功后响应中附加 `token` 字段（非表字段）。

## 2. businesses — 商家

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| name | TEXT | 商家名称 |
| phone | TEXT | 联系电话 |
| introduce | TEXT | 简介 |
| logo | TEXT | Logo URL |
| address | TEXT | 地址 |
| status | TEXT | 审核状态：通过 / 其它 |
| uid | INTEGER | 店主用户 id → users.id |
| open_status | INTEGER | 营业状态：1 营业 0 打烊 |
| min_amount | REAL | 起送价 |
| open_time | TEXT | 营业开始时间 |
| close_time | TEXT | 营业结束时间 |
| score | REAL | 评分（聚合字段） |
| sales | INTEGER | 销量（聚合字段） |

## 3. categories — 商品分类

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| name | TEXT | 分类名称 |
| bid | INTEGER | 所属商家 id → businesses.id |
| b_name | TEXT | 商家名称（冗余字段） |

## 4. products — 商品

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| name | TEXT | 商品名称 |
| description | TEXT | 描述 |
| price | REAL | 原价 |
| sum | INTEGER | 销量／库存 |
| picture | TEXT | 图片 URL |
| bid | INTEGER | 所属商家 id → businesses.id |
| cid | INTEGER | 所属分类 id → categories.id |

> 到手价 = `price × 0.7`，由后端计算后以 `realPrice` 字段返回（非表字段）。

## 5. carts — 购物车

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| pid | INTEGER | 商品 id → products.id |
| num | INTEGER | 数量 |
| uid | INTEGER | 用户 id → users.id |
| bid | INTEGER | 商家 id → businesses.id |

> 同一用户 + 同一商品 + 同一商家为一条记录，重复加购累加 `num`。

## 6. orders — 订单主表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| order_no | TEXT | 订单号（雪花算法生成） |
| pay_type | TEXT | 支付方式：微信 / 支付宝 |
| pay_time | TEXT | 支付时间 |
| status | TEXT | 订单状态（见下方状态说明） |
| bid | INTEGER | 商家 id → businesses.id |
| user | TEXT | 收货人姓名 |
| phone | TEXT | 收货电话 |
| address_id | INTEGER | 收货地址 id → addresses.id |
| uid | INTEGER | 下单用户 id → users.id |
| amount | REAL | 商品原价合计 |
| actual | REAL | 实付金额（7 折后） |
| comment | TEXT | 订单备注 |
| cover | TEXT | 商品封面图（列表展示用） |
| name | TEXT | 商品概要（如「现炒黄焖鸡米饭等 2 件商品」） |
| time | TEXT | 下单时间 |
| comment_status | INTEGER | 评价状态：0 未评 1 已评 |
| did | INTEGER | 接单骑手用户 id → users.id |
| deliveryman_name | TEXT | 骑手姓名 |
| deliveryman_phone | TEXT | 骑手电话 |

**status 状态取值**：`待支付` `待发货` `待收货` `正在配送` `已完成` `已取消` `已退款`

**状态流转**：

```
待支付 ──支付──▶ 待发货 ──发货──▶ 待收货 ──骑手接单──▶ 正在配送 ──确认收货──▶ 已完成
   │                │                                                          │
   └──取消──▶ 已取消  └──退款──▶ 已退款                            评价后 comment_status = 1
```

## 7. ordersitem — 订单明细

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| order_id | INTEGER | 订单 id → orders.id |
| num | INTEGER | 数量 |
| price | REAL | 下单时单价（原价快照） |
| product_name | TEXT | 商品名称（快照） |
| product_picture | TEXT | 商品图片（快照） |

> 明细保存商品快照而非外键，不含 `pid`；金额以 `price × 0.7` 计算后返回 `realPrice`。

## 8. addresses — 收货地址

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| address | TEXT | 详细地址 |
| user | TEXT | 联系人 |
| phone | TEXT | 联系电话 |
| user_id | INTEGER | 所属用户 id → users.id |

## 9. comments — 评论

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| star | REAL | 星级评分（1–5） |
| content | TEXT | 评价内容 |
| time | TEXT | 评价时间 |
| order_id | INTEGER | 订单 id → orders.id |
| uid | INTEGER | 评价用户 id → users.id |
| bid | INTEGER | 商家 id → businesses.id |

## 10. collects — 收藏

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| business_name | TEXT | 商家名称 |
| bid | INTEGER | 商家 id → businesses.id |
| uid | INTEGER | 用户 id → users.id |
| time | TEXT | 操作时间 |
| is_collect | INTEGER | 1 收藏 0 取消（软删除标记） |

## 11. notices — 系统公告

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| title | TEXT | 标题 |
| content | TEXT | 富文本内容 |
| user_id | INTEGER | 发布人 id → users.id |
| time | TEXT | 发布时间 |
| open | BOOLEAN | 是否公开（移动端首页取已公开公告） |

## 12. news — 新闻

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| title | TEXT | 标题 |
| description | TEXT | 摘要 |
| content | TEXT | 富文本内容 |
| author_id | INTEGER | 作者 id → users.id |
| time | TEXT | 发布时间 |

## 13. logs — 操作日志

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| operation | TEXT | 操作模块（如「订单」「用户」） |
| type | TEXT | 操作类型（新增/修改/删除/登录/注册） |
| ip | TEXT | 操作人 IP |
| user | TEXT | 操作人 |
| time | TEXT | 操作时间 |

> 由 Gin 中间件在业务成功后异步写入，非手工录入。

## 14. banners — 首页轮播图

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| img | TEXT | 图片 URL |
| open | BOOLEAN | 是否上架 |
| time | TEXT | 创建时间 |

## 15. deliverymen — 骑手实名认证

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| user_id | INTEGER | 用户 id → users.id |
| name | TEXT | 真实姓名 |
| identification | TEXT | 身份证号（18 位） |
| student_number | TEXT | 学号 |
| phone | TEXT | 联系电话 |
| time | TEXT | 提交时间 |
| status | TEXT | 审核状态：通过 / 待审核 |

---

## 附：表关系概览

```
users ──┬─< businesses (uid)          ──< categories (bid)
        │                              ──< products (bid)
        ├─< addresses (user_id)        ──< carts (bid)
        ├─< carts (uid)                ──< orders (bid)
        ├─< orders (uid)               ──< comments (bid)
        ├─< comments (uid)             ──< collects (bid)
        ├─< collects (uid)
        ├─< deliverymen (user_id)
        └─< notices / news (发布人)

orders ──< ordersitem (order_id)
orders ──   addresses (address_id)
orders ──   users (did，接单骑手)
products ──< carts (pid)
```
